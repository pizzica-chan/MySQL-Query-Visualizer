期待: 要確認（黄）— 結果セットは「要確認」、要確認ヒントあり
ヒント: LIMIT あり・ORDER BY なし
パターン: INNER JOIN の記述順入れ替え + LIMIT 10（ORDER BY なし）
理由: LIMIT 前の行集合は同じとみなされやすいが、ORDER BY がないため LIMIT で返る行は実行依存。
      02（LIMIT なし）とは判定が異なる点に注意。
表示: サマリーは黄、「結果セット：要確認」、注意ボックスと要確認ヒントが出る。
      JOIN 記述順の差分はカテゴリ一覧には出ない（ヒント・本文で案内）。
