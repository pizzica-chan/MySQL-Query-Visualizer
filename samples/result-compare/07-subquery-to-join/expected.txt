期待: 要確認（黄）— 結果セットは「重複を除けば同じ」、等価証明の説明あり
判定: 合接クエリ断片に収まるため、重複を除いた行の集合が一致することを証明済み
理由: 相関 EXISTS を INNER JOIN + DISTINCT に書き換えた半結合パターン。
      集合としては一致するが、片側にしか DISTINCT がないため重複行の個数までは保証されない。
      users の (id, name) に重複行がなければ完全に同じ結果になる。
両方に DISTINCT を付けた場合は 14-exists-to-join-distinct を参照（緑・証明済み）。
